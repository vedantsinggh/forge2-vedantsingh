# Python Language Standards

## Overview
Idiomatic Python (PEP8), type annotations, async/await, and testing conventions.

## Capabilities
- Annotate all function signatures with complete input and return types.
- Follow PEP8 code formatting guidelines (checked via Ruff).
- Use context managers (with statement) to handle open resources safely.
