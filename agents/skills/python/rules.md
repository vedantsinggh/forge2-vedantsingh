# Python Language Coding Rules

- **Rule**: Annotate all function signatures with complete input and return types.
- **Rule**: Follow PEP8 code formatting guidelines (checked via Ruff).
- **Rule**: Use context managers (with statement) to handle open resources safely.
