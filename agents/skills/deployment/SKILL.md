# Deployment & Containers

## Overview
Docker compose, Dockerfiles, environment files, runtime configs.

## Capabilities
- Specify explicit image versions (no latest tags) in Dockerfiles.
- Keep container images lean by using multi-stage builds.
- Declare container healthchecks monitoring standard endpoints.
